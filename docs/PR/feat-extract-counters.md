Title: Extract NumberCounter & Sparkline

This branch extracts shared UI utilities into reusable components:

- `app/components/NumberCounter.tsx`
- `app/components/Sparkline.tsx`

Files updated to import the shared components:

- `app/components/Cloudspace.tsx`
- `app/components/WeatherCardNew.tsx`

Local checks: lint and tests passed locally (71 tests).
